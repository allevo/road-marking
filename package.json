{
  "name": "road-marking",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "unit": "tap -b tests/*.test.js",
    "lint": "standard | snazzy",
    "test": "npm run lint && npm run unit",
    "coveralls": "npm run unit -- --cov",
    "bench": "tap -b --timeout 60000 -Rlist bench.js",
    "coverage": "npm run unit -- --cov --coverage-report=html"
  },
  "author": "Tommaso Allevi <tomallevi@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "benchmark": "^2.1.4",
    "coveralls": "^3.0.2",
    "find-my-way": "^1.15.1",
    "microtime": "^2.1.8",
    "pre-commit": "^1.2.2",
    "snazzy": "^8.0.0",
    "standard": "^12.0.1",
    "tap": "^14.6.9"
  },
  "dependencies": {
    "fast-decode-uri-component": "^1.0.0",
    "generate-function": "^2.3.1"
  }
}
